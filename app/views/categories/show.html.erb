<html>
<head>
  <title><%= @category.name %></title>
</head>

<body>
  <h3>Products tagged <%= @category.name %></h3>
  <% @category.products.each do |product| %>
    <%= render "shared/display_product", product: product %>
  <% end %>
</body>
</html>
